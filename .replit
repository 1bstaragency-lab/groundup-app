run = "npm install --legacy-peer-deps && npx expo start --tunnel"
entrypoint = "app/index.tsx"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm install --legacy-peer-deps && npx expo start --tunnel"]

[[ports]]
localPort = 8081
externalPort = 80
